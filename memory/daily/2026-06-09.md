# 2026-06-09 — CP122 harness verification: all five cells proven live

Final verification pass over the CP122 harness work (commits `875ae1f`, `056d7d2`).
Every cell was exercised live, not just read:

## What was verified (with evidence)

- **Build + tests:** `tsc` clean; `vitest run` → **37/37 passing** across 4 suites
  (pricing, tracker, memory-search, proxy integration with a real round-trip).
- **Memory:** `node dist/index.js memory-search "untrusted pricing sandbox isolation" --limit 3`
  returned BM25-ranked hits with snippets (top hit `memory/MEMORY.md`, score 9.40).
  Index = `src/memory-search.ts` (inverted index, IDF + length normalization +
  source-authority weights ported from Energy's `memory-search.sh`).
- **Skills:** 3 repo-authored domain skills each carry Trigger + Steps + Output —
  `add-model-pricing`, `cost-investigation`, `isolate-untrusted-pricing`.
- **Hooks:** fed real PreToolUse JSON to both hook scripts —
  `ls` → allow `{}`; `rm -rf /` → deny (`rm-root`); Read of `.env` → deny (`env-file`).
  Matchers in `.claude/settings.json` point at the `.cjs` files that ran.
- **Dispatch:** `gh workflow list` shows **Cost Watch — active**; last
  `workflow_dispatch` run on `main` → **completed success (20s)**. Daily cron
  `0 8 * * *` is enabled in `.github/workflows/cost-watch.yml`.
- **Sandbox (live boot):** `agentmoney sandbox-run` with `E2B_API_KEY` from `.env`
  booted a real Firecracker microVM:
  - Sandbox ID `ig2udyhamlv8fvyqe92a8`, boot **1668ms**, run 1121ms, exit **0**
  - In-sandbox total **$0.106000** == host cross-check **$0.106000** (engines agree)

## Durable takeaway

The isolated pricer (`src/sandbox-runner.ts` `IN_SANDBOX_PRICER`) duplicates a minimal
rate table on purpose — the in-sandbox program must not import host modules. When rates
change in `src/pricing.ts`, the sandbox mirror only needs updating if its few spot-check
models change; the host cross-check in `sandbox-run` will surface any drift.
