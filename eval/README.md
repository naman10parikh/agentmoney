# eval/

Eval + observer layer for the agentmoney harness. Add eval suites and observer runs here that
score the agent's own behavior (e.g. golden cost-calculation tasks). Seeded empty — see
[[Ops - Harness]] in `brain/`. Product unit tests live in `test/` (run `npm test`).
